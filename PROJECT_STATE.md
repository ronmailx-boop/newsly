# Project State - Newsly

## תיאור הפרויקט

מרכז חדשות ישראלי המאגד כותרות מכמה מקורות RSS למקום אחד, עם קישור לכתבה
המקורית. סטאק: GitHub Pages (פרונט סטטי) + GitHub Actions (סקרייפינג
מתוזמן) + JSON סטטי בריפו. Firebase/Render יתווספו רק כשיהיה צורך אמיתי
בבקאנד (למשל תכונות משתמש עתידיות).

## החלטות ארכיטקטורה

- **סטטי טהור בשלב זה, לא PWA.** אין Service Worker/manifest - זה מוסיף
  מורכבות (ניהול cache, גרסאות) בלי צורך אמיתי בעבודה אופליין לפיד
  חדשות. המבנה (JSON + פרונט וניל JS ללא build step) מאפשר להוסיף PWA
  בעתיד כשכבה נוספת, לא כשינוי ארכיטקטוני.
- **בלי build step בפרונט** - HTML/CSS/JS וניל ישירות ב-GitHub Pages, כדי
  שיהיה קל לערוך ולבדוק ישירות מהמובייל בלי צורך במחשב/bundler.
- **GitHub Action כותב JSON וה-commit-back ל-`data/news.json`** - בדיוק
  כמו הסקרייפר הקודם (Wizzair) שכבר קיים אצל המשתמש.

## סטטוס משימות

- [x] מבנה פרויקט (package.json, .gitignore, .env.example)
- [x] `scripts/sources.mjs` - הגדרת מקורות RSS (ynet, וואלה, מעריב),
      מובנה כך שקל להוסיף מקור RSS נוסף או מקור מסוג `playwright`
- [x] `scripts/fetch-news.mjs` - שליפה מקבילית, ניקוי HTML מתקצירים,
      דה-דופליקציה בסיסית לפי כותרת מנורמלת, גיזום לפי `KEEP_DAYS` (5
      ימים כברירת מחדל), מיזוג עם הקובץ הקודם, מיון לפי `pubDate`
- [x] נבדק מקומית מול שרת RSS מדומה (localhost) - הלוגיקה עובדת: ניקוי
      HTML, דה-דופ, גיזום, מיון. **לא נבדק מול המקורות האמיתיים** כי
      לסביבת הפיתוח הנוכחית אין גישת רשת אליהם - הריצה הראשונה בפרודקשן
      (GitHub Actions) היא הבדיקה האמיתית מול ynet/וואלה/מעריב.
- [x] `.github/workflows/fetch-news.yml` - רץ כל 15 דקות + `workflow_dispatch`
      להרצה ידנית, מבצע commit-back רק אם יש שינוי בפועל
- [x] `index.html` + `css/style.css` + `js/app.js` - פיד מובייל-first,
      RTL, ערכת נושא סגולה (#7367f0 / #7c4ddb), טאבים לסינון לפי מקור,
      טיפול בשגיאות רשת עם הודעה בעברית
- [x] **תצוגת Reels (הוחלף מ-feed רגיל לבקשת המשתמש):** כל כותרת תופסת
      מסך מלא, גלילה אנכית עם `scroll-snap`. ב"הכל" הכותרות מעורבבות
      בין מקורות (round-robin) כך שה-reel הבא תמיד ממקור שונה מקודמו.
      גלילה אוטומטית עם בחירת מהירות (2/5/10/20 שנ' או כבוי) דרך פאנל
      הגדרות (⚙️), נשמרת ב-`localStorage`. גלילה ידנית וגלילה אוטומטית
      עוברות באותו נתיב קוד (`IntersectionObserver` קובע מי ה-reel
      הפעיל ומאפס את הטיימר), כך שאין קפיצות/קונפליקטים בין השניים.
- [x] נבדק ויזואלית עם Playwright (מובייל 390px): interleaving בין
      מקורות עובד (ynet→וואלה→מעריב), סינון טאב יחיד לא מערבב (ynet
      בלבד מציג ynet ברצף), פאנל ההגדרות נפתח כראוי, ושינוי מהירות
      ל-2 שנ' מקדם reel אוטומטית כצפוי אחרי 3 שניות. אין שגיאות קונסול.
- [x] מסמכי `docs/legal/*` (4 קבצים) עם placeholders
- [x] `PROJECT_STATE.md` (קובץ זה)
- [x] **מוזג ל-`main`** (fast-forward, ללא קונפליקטים) - נדרש כי GitHub
      Pages ו-scheduled/dispatch workflows מזהים רק קבצים שקיימים ב-branch
      ברירת המחדל.
- [x] **ה-Action `fetch-news.yml` נבדק בפרודקשן (workflow_dispatch ידני)
      ועבד מקצה לקצה מול המקורות האמיתיים** - נשלפו והתמזגו 76 כותרות
      אמיתיות משלושת המקורות (ynet/וואלה/מעריב), עם ניקוי HTML, מיון
      לפי `pubDate` ו-commit-back אוטומטי על ידי `github-actions[bot]`
      (commit `c874da2`). ה-cron (כל 15 דק') ירוץ מעכשיו אוטומטית.
- [x] **"מה הכותרת מסתירה?" (פענוח קליקבייט, Clickbyter)** (לבקשת
      המשתמש, 2026-09-14): כפתור על כל reel שקורא ל-Worker חיצוני של
      `ronmailx-boop/clickbyter` (אותו host, CORS עובד בלי שינוי בצד
      השני) - על פי דרישה בלבד, לא אוטומטי. פירוט מלא תחת "Current
      Focus" למעלה ובסעיף Reels ב-`CLAUDE.md`.
- [x] **מחיקת כותרת ב-swipe + מסך אישור מעוצב** (לבקשת המשתמש,
      2026-09-14): swipe אופקי על reel + מסך אישור מותאם (לא
      `window.confirm`) לפני הסתרה, מקומי ל-`localStorage` בלבד.
      פירוט מלא תחת "Current Focus" למעלה ובסעיף Reels ב-`CLAUDE.md`.
- [x] **אייקון אפליקציה הוחלף** (לבקשת המשתמש, לא רקע סגול): הוצג קנבס
      עיצוב עם 10 כיווני אייקון שונים (Claude Design preview), המשתמש
      בחר "כותרות גלובליות" (גלובוס בקווי זהב + פסי כותרת לבנים על
      נייבי `#0c1e3e`). `icons/icon-192.png` ו-`icons/icon-512.png`
      עודכנו (ריבוע מלא, לא מעוגל בקובץ עצמו - האנדרואיד מפעיל מסכה
      משלו; התוכן נבדק שנשאר בתוך "אזור בטוח" של כ-80% מהרוחב/גובה).
      `manifest.json` לא שונה - עדיין מצביע על אותם שני קבצים.
- [x] **"טקסט מלא" בתוך ה-reel** (לבקשת המשתמש: לקרוא את הכתבה בלי
      לצאת ממנה): נבדקה ונדחתה גישת גירוד (scraping) של הכתבה המלאה
      מאתר המקור - סיכון ממשי של הפרת זכויות יוצרים (עקיפת מודל
      ההכנסות של המו"לים) ובעיות אנטי-בוט/ToS, בדומה לסיבות שכבר דחו את
      N12/ביזפורטל/דה מרקר. הפתרון שיושם (המשתמש אישר): תקציר ה-RSS
      מוצג **במלואו** (לא חתוך יותר ל-220 תווים - `MAX_SUMMARY_LENGTH`
      הועלה ל-3000 ב-`fetch-news.mjs`, כתקרת בטיחות בלבד).
      **גרסה ראשונה** הוסיפה כפתור "הצג טקסט מלא" + מודל נפרד - המשתמש
      בדק וגילה שההבדל בפועל בין הטקסט הקצר למלא הוא בסך הכל כמה מילים,
      ושיש מספיק מקום בתוך ה-reel עצמו. **לכן הוסר הכפתור/המודל לגמרי**
      והתקציר המלא מוצג ישירות בתוך `.reel__summary` (בלי line-clamp).
      ל-`.reel` נוסף `overflow-y: auto` כרשת ביטחון בלבד למקרה קיצון של
      תקציר ארוך שלא ייכנס במסך. נבדק ויזואלית עם Playwright (גם עם
      טקסט ארוך מלאכותית) - נכנס בנוחות במסך אחד, אין שגיאות קונסול.
- [ ] **לאמת ידנית:** GitHub Pages הופעל ובנה deploy אחד לפני המיזוג
      (מה-commit הישן, בלי `index.html`). מסביבת הפיתוח הנוכחית אין
      גישת רשת ל-`*.github.io` כדי לאמת שה-deploy החדש עלה בפועל - יש
      לבדוק ידנית ב-https://ronmailx-boop.github.io/newsly/ שהאתר מציג
      את פיד החדשות ולא עמוד ריק/ישן. אם לא התעדכן אוטומטית תוך כמה
      דקות מה-push ל-main, לבדוק ב-Settings → Pages שהמקור עדיין מוגדר
      ל-`main` / root, ואפשר לדחוף שינוי טריוויאלי כדי לטריגר deploy חדש.

- [x] **נבדקו 3 מקורות נוספים שביקש המשתמש (גלובס, כלכליסט, N12) - כולם
      נבדקו בפועל מול ריצות אמיתיות של ה-Action (לא רק בבדיקה מקומית)
      ונדחו/הוסרו לאחר אבחון אמיתי:**
      - **N12:** מוגן ע"י **Radware Bot Manager**. הניסיון לשלוף עם
        Playwright headless חזר עם עמוד "not a bot" של Radware (עם
        IP/trace ID לפנייה לביטול חסימה) - כלומר האתר חוסם באופן פעיל
        גישה אוטומטית ברמת התשתית. **לא ניסיתי לעקוף את זה** (זה לא
        באג בסלקטור אלא הגנת אנטי-בוט מכוונת של האתר) - הוסר לגמרי
        מהקוד (גם ה-dependency של Playwright, גם ה-workflow step).
      - **כלכליסט:** אין RSS ציבורי שהצלחתי לאתר - 5 כתובות מנוחשות
        לפי דפוסי URL נפוצים (כולל דפוס ynet, בהנחה ששתיהן בקבוצת
        ידיעות) החזירו כולן HTTP 404 בפועל, וגם חיפוש תג
        `<link rel="alternate" type="application/rss+xml">` בעמוד הבית
        לא העלה תוצאה. הוסר מהקוד.
      - **גלובס:** ה-URL הידוע (`FeederNode?iID=942`, שכותרתו "Front -
        Globes") **כן מחזיר XML תקין**, אבל התאריכים בו לא כרונולוגיים
        בזמן אמת - בריצה אחת הפריט העדכני ביותר היה מ-3 בספטמבר, ובריצה
        מאוחרת יותר (אותה שעה!) מ-31 באוגוסט - כל הפריטים מבוגרים מ-5
        הימים (`KEEP_DAYS`) ונגזמים בשקט. ניסיתי לאתר את מזהה ה-feed
        הנכון דרך `https://www.globes.co.il/news/RSS.tag` (עמוד ה-RSS
        הרשמי של גלובס) אבל בלי גישת דפדפן אמיתית לא הצלחתי לפרש את
        המבנה בוודאות. הוסר מהקוד.
      - רוטר לא נוסף כלל בכוונה - פורום גולשים, לא מתאים למודל
        "כותרת+קישור".
      - **אם המשתמש רוצה להוסיף אחד מהם בעתיד:** הדרך המהירה ביותר היא
        שהוא עצמו יפתח את עמוד ה-RSS הרשמי של האתר (למשל
        `calcalist.co.il/tags/rss` או `globes.co.il/news/RSS.tag`)
        בדפדפן במובייל, ויעתיק את כתובת ה-XML הנכונה - במקום שאני אנחש
        אותה בלי גישת רשת ישירה.

## איך מוסיפים מקור RSS חדש

1. להוסיף אובייקט חדש למערך `sources` ב-`scripts/sources.mjs`:
   `{ key, name, type: 'rss', url }`.
2. להוסיף כפתור טאב תואם ב-`index.html` (`data-source` = ה-`key`).
3. זהו - אין צורך בשינוי לוגיקה נוספת.

## איך מוסיפים מקור עתידי שדורש Playwright (למשל N12/מאקו)

1. להוסיף מקור עם `type: 'playwright'` ב-`sources.mjs`.
2. לכתוב פונקציית `fetchPlaywrightSource(source)` ב-`fetch-news.mjs`
   שמחזירה אותה צורת אובייקט (`title`, `link`, `summary`, `source`,
   `sourceKey`, `pubDate`) כמו `fetchRssSource`.
3. להוסיף את הענף המתאים ב-`fetchAllSources()`.
4. להוסיף את חבילת Playwright כ-dependency ולוודא שה-workflow מתקין
   דפדפנים (`npx playwright install --with-deps chromium`).

## דברים שלא נכנסו לגרסה הראשונה (בכוונה)

- רוטר - פורום גולשים, לא מתאים למודל "כותרת+קישור" (הוחלט סופית לוותר).
- PWA (manifest + Service Worker) - לא נדרש כרגע, ראו "החלטות ארכיטקטורה".
- דה-דופליקציה מתקדמת (fuzzy matching בין ניסוחים שונים לאותו אירוע) -
  יש רק דה-דופ בסיסי לפי כותרת מנורמלת. קל להרחיב בעתיד (למשל בדיקת
  דמיון בין תקצירים) אם יתגלו הרבה כפילויות בפועל.

## היסטוריה - מקורות נוספים שנבדקו ונדחו

המשתמש ביקש להוסיף גלובס, כלכליסט ו-N12. בדקתי את כולם בפועל מול
ריצות אמיתיות של ה-Action (לא ניחוש) ושלושתם נדחו מסיבות אמיתיות
שונות - ראו פירוט מלא ב"סטטוס משימות" למעלה. בקצרה: N12 חסום ע"י
מנגנון אנטי-בוט (Radware) ולא ניסיתי לעקוף את זה; כלכליסט - אין RSS
ציבורי שאיתרתי; גלובס - ה-feed שמצאתי לא כרונולוגי/לא מתעדכן. הקוד נקי
מהניסיונות האלה. אם המשתמש ירצה לנסות שוב עם כתובת RSS מדויקת (שהוא
ימצא בעצמו בדפדפן), זו תוספת של שורה אחת ב-`scripts/sources.mjs` +
כפתור טאב ב-`index.html`.

## היסטוריה - בעיית פריסת Pages (נפתרה לצמיתות)

GitHub Pages (במצב "Deploy from a branch") הפסיק להתעדכן אוטומטית
אחרי כל push שלי - קרה פעמיים. הסיבה: pushes שנעשים דרך זהות בוט לא
מפעילים את מנגנון ה-auto-build הישן של Pages. **הפתרון הקבוע:** הוספתי
`.github/workflows/deploy-pages.yml` שפורס דרך GitHub Actions, מופעל
גם ב-`push` ל-`main` וגם ב-`workflow_run` אחרי סיום `Fetch news` (כי
commit-ים עם `GITHUB_TOKEN` פנימי לא מפעילים `on: push` ב-workflows
אחרים - הגנת GitHub מפני לולאות). המשתמש ביצע את שינוי ה-Settings
הנדרש (Source → GitHub Actions) - **אושר עובד, כולל ב-Chrome.**

## Current Focus

**עודכן (2026-09-14): שילוב חדש - "מה הכותרת מסתירה?" (פענוח קליקבייט דרך Clickbyter).**

המשתמש הוסיף לסשן את הריפו הנפרד `ronmailx-boop/clickbyter` (PWA
נפרד: `public/` סטטי + `worker/` Cloudflare Worker שקורא כתבה מהמקור
ומריץ Groq AI כדי להחזיר את מה שכותרת קליקבייט "מסתירה") וביקש לשלב
אופציה דומה בתוך Newsly, מעל הכותרת, כתוספת למה שכבר קיים.

**נחקר ותועד:** ה-Worker כבר מוגדר ל-CORS לפי `ALLOWED_ORIGIN =
"https://ronmailx-boop.github.io"` (host בלבד, לא path) - ומאחר ש-Newsly
מתארח תחת אותו host (`/newsly/`), אפשר לקרוא ל-API הקיים
(`POST https://clickbyter-api.ronmailx.workers.dev/api/decode`)
**ישירות מ-Newsly בלי שום שינוי בצד clickbyter**. הוסבר למשתמש שפענוח
אוטומטי לכל כותרות הפיד לא מעשי (כל קריאה שולפת כתבה מלאה + Groq -
עד ~24 שניות, וה-Worker מוצהר בלי rate limiting) - המשתמש בחר באופציה
המומלצת: כפתור על פי דרישה בכל reel.

**יושם ונבדק (Playwright, עם mock ל-fetch כי הרשת בסביבת הפיתוח חסומה
לדומיין הזה):**
- כפתור "🔍 קליקבייט? לחץ כאן" מעל כל כותרת (`.reel__decode`, אלמנט
  אחות ל-`<a>` הראשי - לא מקונן בתוכו, כמו כפתור המחיקה). לחיצה →
  מצב טעינה → תשובה (callout סגול בהיר) או שגיאה בעברית + "נסה שוב".
- תוצאות נשמרות ב-`Map` בזיכרון (`decodedAnswers`, לא `localStorage` -
  זמני לסשן) לפי `item.id`, כדי שמעבר טאבים/רינדור מחדש לא יגרום
  לקריאת API כפולה מיותרת לאותה כתבה.
- `.reel` חזר להיות מבנה עם `<a class="reel__link">` פנימי (לא
  `.reel` עצמו) - כמו בפיצ'ר "טקסט מלא" הראשון (שהוסר), כדי לאפשר
  את כפתור הפענוח כאלמנט אחות ולא button-בתוך-a. נבדק שה-swipe-delete
  וה-tap-לפתיחת-כתבה ממשיכים לעבוד תקין עם המבנה החדש.
- אין שגיאות קונסול; מסך "אישור מחיקה" והתגובתיות הקיימת לא נפגעו.

**מגבלה מוצהרת:** פענוח דורש שהאתר המקורי יהיה נגיש לגירוד (לא חסום
אנטי-בוט) - לא כל כתבה תצליח (בדומה למגבלות שכבר ידועות מ-N12/
ביזפורטל/דה מרקר), וזה תלוי בזמינות/מכסת ה-Worker והחשבון של Groq
שהמשתמש מחזיק בפרויקט clickbyter, לא משהו ש-Newsly שולט בו.

### היסטוריה - מסך אישור מחיקה הוחלף לעיצוב מותאם (2026-09-14, מוקדם יותר)

המשתמש ראה את ה-`window.confirm()` הדפדפני הגנרי ("האתר אומר...") וביקש
עיצוב יותר מרשים. הוחלף ב-`#confirm-dialog` (`index.html`) - מודל אמיתי
עם אייקון פח אשפה עגול אדום (SVG, לא emoji), כרטיס לבן עם אנימציית
fade+scale, שני כפתורים (אדום "הסתרה" / outline "ביטול"). נגישות מלאה:
`role="alertdialog"`, `aria-modal`, `aria-describedby` עם שם הכותרת
הספציפית, לכידת פוקוס בין 2 הכפתורים, Escape/קליק-רקע = ביטול, פוקוס
פותח על "ביטול" (ברירת מחדל בטוחה). `askConfirmDelete()` ב-`js/app.js`
מחזיר `Promise<boolean>` - קוד הקריאה (`confirmAndDeleteReel`) הפך
ל-`async`. עוצר/מפעיל autoplay סביב הדיאלוג. **נבדק עם Playwright**
(לא רק לוגית - כולל focus/Tab-trap/Escape/backdrop/כפתורים): אין
קריאה ל-`window.confirm` בכלל יותר, פוקוס פותח נכון, Tab עובר בין
שני הכפתורים ועוטף, Escape מבטל בלי למחוק, לחיצה על "הסתרה" מוחקת
בפועל. אין שגיאות קונסול.

### היסטוריה - מחיקת כותרת ב-swipe (2026-09-14, מוקדם יותר באותו יום)

המשתמש ביקש: swipe ימינה/שמאלה על reel למחיקת כותרת שלא רוצים לראות
יותר, עם התראה (אישור) לפני המחיקה בפועל. יושם ונבדק:
- swipe אופקי (בכל כיוון) מזיז את ה-reel עם האצבע (`translateX` +
  דהיית opacity) - מעל 100px נפתח מסך אישור לפני הסתרה בפועל (בגרסה
  הראשונה: `window.confirm`, אחר כך הוחלף - ראו מעלה); ביטול = חוזר
  למקום. גלילה אנכית רגילה (לניווט בין reels) לא מושפעת - הבחנה לפי
  יחס dx/dy, ו-`touch-action: pan-y` מונע התנגשות.
- ההסתרה **מקומית בלבד** (`localStorage`, לא `data/news.json`) - לא
  משפיעה על משתמשים אחרים, לא דורשת backend. שורדת רענון/טעינה מחדש.
  מזהים מוסתרים שיצאו מהפיד (אחרי `KEEP_DAYS`) נגזמים אוטומטית כדי
  שהאחסון לא יגדל בלי גבול.
- **נבדק עם Playwright (סימולציית touch events אמיתית, לא רק לוגיקה):**
  swipe קטן מדי (50px) - לא מוחק; תזוזה אנכית-דומיננטית - לא מוחק;
  swipe מלא + ביטול - לא מוחק, חוזר למקום; swipe מלא + אישור - מוחק
  בפועל, שאר הכותרות תקינות ו-`data-index` מתעדכן נכון; המזהה נשמר
  ב-localStorage; **שורד רענון דף** (הכותרת נשארת מוסתרת).
- **מגבלה מוצהרת:** אין חלופה נגישה-מקלדת לפתיחת המחיקה עצמה (ה-swipe
  עצמו) - המשתמש ביקש ספציפית swipe, לא כפתור נוסף על המסך (בדומה
  להחלטה קודמת נגד כפתור "טקסט מלא" נפרד) - אבל מסך האישור עצמו כן
  נגיש-מקלדת במלואו (ראו מעלה).

### היסטוריה - בעיית אמינות ה-cron (2026-09-13, מוקדם יותר)

**עודכן (2026-09-13, בדיקה נוספת מאוחר יותר): הקצב האוטונומי אומת יציב על פני כמה מחזורים, לא רק פעם אחת.**

המשתמש שאל שוב "עובד כל 15 דקות?" - נבדק ישירות מול GitHub 4 ריצות
רצופות אחרונות (כולן `event: workflow_dispatch`, כלומר מ-cron-job.org):
run #22 (13:34, הידני), #23 (13:45, +11.1 דק'), #24 (14:00, +15.2 דק'),
#25 (14:15, +14.8 דק'). מ-#23 והלאה (הראשונות שרצו לגמרי לבד, בלי שום
מגע ידני) - הפער יציב סביב 15 דקות בדיוק. **מאושר: זה עובד באמינות
מתמשכת, לא רק בבדיקה חד-פעמית.**

**עודכן (2026-09-13): בדיקת מקצה-לקצה מלאה אחרי תיקון אמינות ה-cron - הכל ירוק.**

- **ה-trigger החיצוני אומת כאוטונומי בפועל, לא רק ב-test run ידני:**
  חוכה בזמן אמת לריצה הבאה של cron-job.org (בלי שום לחיצה/הפעלה ידנית
  מהצד שלי או של המשתמש) - וזו אכן קרתה לבד: run #23,
  `event: workflow_dispatch`, ב-13:45:12 UTC (בדיוק 11 דק' אחרי ה-test
  run הידני הקודם, תואם ל"כל 15 דקות"), הסתיימה בהצלחה. מיד אחריה
  `Deploy Pages` run #31 (`event: workflow_run`) הופעל אוטומטית ועבר -
  כלומר השרשרת המלאה (cron-job.org → GitHub API → fetch-news →
  commit → auto-deploy) עובדת מקצה לקצה **בלי שום התערבות ידנית**.
- **תוכן `data/news.json` אומת ישירות מ-GitHub:** 426 כותרות, כל 6
  המקורות מיוצגים (israelhayom 193, maariv 64, walla 60, ynet 55,
  kikar 50, hakolhayehudi 4). 53/426 עדיין עם תקציר ישן חתוך + "…"
  (מלפני תיקון ה-summary length) - מספר הולך ויורד עם כל ריצה, וימשיך
  לרדת עכשיו הרבה יותר מהר בזכות ריצות אמינות כל 15 דק'.
- **טאבים בפרונט אומתו תואמים בדיוק** ל-sourceKeys (all/ynet/walla/
  maariv/israelhayom/kikar/hakolhayehudi).
- **נבדק ויזואלית עם Playwright מול הנתונים האמיתיים** (הורדו ישירות
  מ-GitHub, לא מדומים): "הכל" מציג כותרת טרייה עם "עכשיו" ותקציר מלא
  ללא חיתוך, סינון "הקול היהודי" מציג רק את הפריטים שלו. אין שגיאות
  קונסול.
- **הפתרון ל-cron מתועד במלואו למטה ("בעיית אמינות ה-cron").**

### היסטוריה - בעיית אמינות ה-cron אובחנה ותוקנה (2026-09-13, מוקדם יותר)

בעקבות שאלת המשתמש "למה יש כתבות עם '...'" התגלתה בעיה עמוקה יותר:
- **אובחן בפועל** (הרצת workflow_dispatch עם לוג אבחון זמני + השוואת
  היסטוריית ריצות): ה-cron המוגדר (`*/15 * * * *`) **לא רץ בפועל כל 15
  דקות** - הפערים האמיתיים בין ריצות scheduled היו 1.7 עד 5.75 שעות.
  זו מגבלה ידועה של GitHub Actions (`schedule` הוא best-effort, יכול
  לדלג ריצה שלמה בלי הודעה תחת עומס, בעיקר ב-repo עם פעילות נמוכה) -
  אושר בחיפוש רשת מול תיעוד/דיוני קהילה עדכניים.
- **תוקן חלקית בקוד:** `.github/workflows/fetch-news.yml` - ה-cron
  הוזז מ-`*/15` ל-`7,22,37,52 * * * *` (נמנע מהשעה/רבעי השעה העגולים,
  שם העומס הכי גבוה) - נשאר כגיבוי, ראו מטה.
- **תוקן במלואו - הוקם trigger חיצוני אמין (בוצע ע"י המשתמש, 2026-09-13):**
  חשבון חינמי ב-cron-job.org עם cronjob בשם "Newsly fetch-news trigger"
  שקורא כל 15 דקות (POST) ל-`https://api.github.com/repos/
  ronmailx-boop/newsly/actions/workflows/fetch-news.yml/dispatches`
  עם body `{"ref":"main"}` והדרים: `Authorization: Bearer <Fine-grained
  PAT מצומצם - "Actions: Read and write" רק על ה-repo הזה>`,
  `Accept: application/vnd.github+json`, `X-GitHub-Api-Version:
  2022-11-28`, `Content-Type: application/json`. **אומת מקצה לקצה** -
  test run מ-cron-job.org החזיר `204 No Content`, ומול GitHub עצמו
  נראתה ריצה אמיתית (run #22, `event: workflow_dispatch`, branch
  `main`) שנוצרה באותה שנייה. הטוקן **לא** נשמר ב-repo או ב-GitHub
  Secrets - הוא רק בהגדרות ה-Headers של הג'וב ב-cron-job.org (ככה
  צריך - הוא לא נקרא מתוך workflow קיים, אלא מפעיל אותו מבחוץ). ה-
  `schedule` הפנימי של GitHub נשאר מוגדר כגיבוי (בטוח לרוץ יחד עם
  ה-trigger החיצוני בזכות `concurrency: group: fetch-news`).
- **הקשר לשאלה המקורית - תוקן/הובהר ב-2026-09-13 מאוחר יותר:** בדיקה
  בפועל (השוואת שני snapshots עוקבים אחרי ריצה אוטונומית) הראתה **0
  תקצירים ישנים התעדכנו** - 52/53 הישנים נשארו זהים בדיוק. הסיבה:
  כתבות ישנות (שעות/יום) כבר לא מופיעות בפיד ה-RSS החי של המקור (יש
  יותר מדי כתבות חדשות שדוחקות אותן), ולכן אין הזדמנות "לתפוס" ולעדכן
  אותן שוב - זה **לא** קשור לתדירות הריצות. מה שכן תוקן: **אף תקציר
  חדש לא נחתך יותר** (4/4 הכתבות החדשות בריצה שנבדקה תקינות במלואן).
  התקצירים הישנים ילכו וייעלמו רק דרך גיזום `KEEP_DAYS` (5 ימים)
  הרגיל - לא דרך "ריענון" - עד אז הם פשוט יישארו עם הטקסט הישן.

### היסטוריה - אייקון חדש + "טקסט מלא" בתוך ה-reel (2026-09-13, מוקדם יותר)

- **אייקון:** הוחלף מהעיצוב הסגול הישן ל"כותרות גלובליות" (גלובוס זהב
  על נייבי) לבקשת המשתמש, שלא רצה רקע סגול לאייקון. פורסם דרך קנבס
  Claude Design עם 10 אפשרויות לבחירה, המשתמש בחר אחת. הקבצים בפועל
  (`icons/icon-192.png`, `icons/icon-512.png`) הוחלפו ונדחפו ל-`main`.
- **"טקסט מלא":** המשתמש ביקש לקרוא כתבה מלאה בתוך ה-reel בלי לצאת
  לאתר המקור. הוסבר למשתמש הסיכון בגירוד תוכן מוגן מהאתר (זכויות
  יוצרים/ToS) - המשתמש בחר באפשרות הבטוחה: הצגת תקציר ה-RSS **במלואו**
  (לא רק 220 תווים). **גרסה ראשונה** הייתה כפתור+מודל נפרד; אחרי שהמשתמש
  בדק בפועל וגילה שההבדל הוא כמה מילים בודדות ושיש מספיק מקום בתוך
  ה-reel עצמו, הכפתור/המודל הוסרו והתקציר המלא מוצג ישירות בתוך ה-reel.
  ראו פירוט טכני מתחת ל-"סטטוס משימות" למעלה ובסעיף Reels ב-`CLAUDE.md`.
- **דבר שממתין למשתמש (לא השתנה):** אישור שהתקנת ה-PWA עובדת בפועל
  במכשיר Android/Chrome.

### היסטוריה - בדיקת מקצה-לקצה מלאה (2026-09-12, אחרי הוספת 3 מקורות)

**הכל ירוק:**

- **הצינור האוטומטי מוכח עצמאי:** ה-cron (`Fetch news`, כל 15 דק')
  רץ לבד ללא התערבות (`event: schedule`, run #15), הצליח, ועשה
  commit. מיד אחריו `Deploy Pages` הופעל אוטומטית דרך `workflow_run`
  (run #16) והצליח - בדיוק כמו שתוכנן. **אין יותר צורך בהתערבות ידנית
  לאף חלק בשרשרת.**
- **תוכן `data/news.json` אומת ישירות מ-GitHub (לא רק מהלוגים):** JSON
  תקין, 267 כותרות, עדכון אחרון 19:38 UTC. כל 6 המקורות מיוצגים עם
  כותרות וקישורים אמיתיים: israelhayom 102, kikar 50, walla 41,
  ynet 38, maariv 30, hakolhayehudi 6 (מספר נמוך יחסית כי רוב
  ה-13,590 הפריטים בארכיון שלו ישנים מ-5 ימים ונגזמים - תקין).
- **טאבים בפרונט אומתו תואמים בדיוק** ל-sourceKeys בנתונים (all/ynet/
  walla/maariv/israelhayom/kikar/hakolhayehudi) - אין טאב יתום ואין
  מקור בלי טאב.
- **נבדק ויזואלית עם Playwright מול הנתונים האמיתיים** (לא מדומים):
  תצוגת "הכל" מציגה כותרת טרייה עם כל העיצוב תקין (RTL, טאבים, badge
  מקור, זמן יחסי), וסינון לפי "הקול היהודי" מציג רק את הפריטים שלו.
  **אין שגיאות קונסול בכלל.**
- תצוגת **Reels** (כותרת אחת במסך מלא, scroll-snap, interleaving בין
  מקורות, autoplay עם מהירות נבחרת 2/5/10/20 שנ'/כבוי) - עובד
- **Screen Wake Lock:** המסך לא נכבה/ננעל בזמן autoplay פעיל - עובד
- **PWA installable:** manifest + אייקונים + service worker מינימלי -
  נבדק מקומית (manifest/SW נטענים תקין). **טרם אושר ע"י המשתמש שההתקנה
  בפועל מ-Chrome/Android עובדת מקצה לקצה** - זו המשימה הפתוחה היחידה.

### מקורות שנבדקו ונדחו (לא לנסות שוב בלי מידע חדש)

ראו הערות בראש `scripts/sources.mjs` לפירוט מלא. בקצרה:
- **גלובס** - feed לא כרונולוגי (תאריכים קופצים אחורה בין ריצות)
- **כלכליסט** - אין RSS ציבורי שאותר
- **N12, ביזפורטל, דה מרקר** - חסומים ע"י הגנת אנטי-בוט/WAF (403 גם על
  דף הבית הרגיל) - **לא לנסות לעקוף**. אם ירצו אחד מהם בעתיד, צריך
  שיתוף פעולה רשמי עם האתר (API), לא scraping.

אין משימה פתוחה כרגע.
